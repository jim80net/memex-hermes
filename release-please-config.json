{
  "$schema": "https://raw.githubusercontent.com/googleapis/release-please/main/schemas/config.json",
  "packages": {
    ".": {
      "release-type": "python",
      "bump-minor-pre-major": true,
      "bump-patch-for-minor-pre-major": true,
      "include-component-in-tag": false,
      "extra-files": [
        {
          "type": "generic",
          "path": "package.json"
        },
        {
          "type": "generic",
          "path": "plugin.yaml"
        }
      ]
    }
  },
  "$comment": "release-type=python drives version bumps in pyproject.toml. extra-files keep the TypeScript package.json and plugin.yaml manifests in lock-step. Both are searched for the literal `x-release-please-version` marker; without the marker release-please leaves them alone (so adding a marker is the opt-in)."
}
