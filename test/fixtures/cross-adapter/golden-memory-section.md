## Coding standard
Always do complete, correct work — never cut corners. Verify by reading the code, running the tests, and tracing the runtime path.
Triggers: "what are the coding standards", "how should I approach a change"

## Commit discipline
Commit changes with a well-written message as the last step.
Triggers: "when do I commit", "commit message style"
